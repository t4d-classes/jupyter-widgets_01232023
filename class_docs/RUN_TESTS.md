# Run Python Tests

1. Install the `pytest` and `nbval` packages.

```bash
conda install -c conda-forge pytest nbval
```

2. Run the tests.

```bash
pytest
```

# Run JavaScript Tests

1. Run the tests.

```bash
yarn test
```